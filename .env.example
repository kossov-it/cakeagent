# Required
TELEGRAM_BOT_TOKEN=           # From @BotFather
TELEGRAM_CHAT_ID=             # Your Telegram user ID (message @userinfobot)

# Claude authentication (one of these):
# Option 1: Run `claude login` on the server (uses browser OAuth)
# Option 2: Set an API key here:
# ANTHROPIC_API_KEY=sk-ant-...

# Everything else is configured via Telegram chat after first run
